# eShopping 專案 PPT 製作說明

## 檔案說明

1. **eShopping_專案簡報.html** - HTML 格式的簡報，可直接在瀏覽器中開啟，或匯入 PowerPoint
2. **create_presentation.py** - Python 腳本，可自動生成 PPTX 檔案（需安裝 python-pptx）

## 方法一：使用 HTML 檔案（推薦）

### 步驟 1：開啟 HTML 檔案
- 用瀏覽器開啟 `eShopping_專案簡報.html`
- 或使用 PowerPoint：檔案 → 開啟 → 選擇 HTML 檔案

### 步驟 2：插入截圖
1. 在每個投影片的左側區域，點擊或選取該區域
2. 插入對應的畫面截圖：
   - 首頁截圖 → 第 3 張投影片
   - 登入/註冊頁面 → 第 4 張投影片
   - 商品列表/詳情 → 第 5 張投影片
   - 購物車頁面 → 第 6 張投影片
   - 訂單相關頁面 → 第 7 張投影片
   - 後台管理頁面 → 第 8-12 張投影片

### 步驟 3：儲存為 PPTX
- 在 PowerPoint 中：檔案 → 另存新檔 → 選擇 .pptx 格式

## 方法二：使用 Python 腳本

### 安裝依賴
```bash
pip3 install python-pptx
# 或
python3 -m pip install python-pptx
```

### 執行腳本
```bash
python3 create_presentation.py
```

### 插入截圖
生成的 PPTX 檔案中，每個投影片左側都有文字佔位符，需要手動替換為實際截圖：
1. 開啟生成的 `eShopping_專案簡報.pptx`
2. 刪除左側的文字框
3. 插入 → 圖片 → 選擇截圖檔案
4. 調整圖片大小和位置

## 建議的截圖清單

1. **首頁** (`/home`) - 展示熱門商品、分類導航
2. **登入頁面** (`/login`) - 用戶認證介面
3. **註冊頁面** (`/register`) - 用戶註冊表單
4. **商品列表** (`/shop`) - 商品展示與搜尋
5. **商品詳情** (`/shop/product/{id}`) - 商品詳細資訊
6. **購物車** (`/cart`) - 購物車內容
7. **結帳頁面** (`/orders/checkout`) - 訂單建立流程
8. **訂單列表** (`/orders/my`) - 用戶訂單歷史
9. **訂單詳情** (`/orders/{id}`) - 訂單完整資訊
10. **後台儀表板** (`/admin/dashboard`) - 管理後台首頁
11. **商品管理** (`/products`) - 後台商品 CRUD
12. **用戶管理** (`/users`) - 後台用戶管理
13. **訂單管理** (`/orders`) - 後台訂單管理

## 投影片結構

每張投影片採用左右分欄設計：
- **左側（48%）**：畫面截圖
- **右側（48%）**：功能描述文字

## 注意事項

1. 截圖建議使用 PNG 或 JPG 格式
2. 截圖解析度建議至少 1920x1080
3. 確保截圖清晰，文字可讀
4. 可以裁剪截圖，只保留重要部分
5. 建議使用瀏覽器的開發者工具截圖功能，或使用截圖工具（如 Snipping Tool、Greenshot）

## 投影片內容說明

### 標題頁
- 專案名稱：eShopping 電商系統
- 副標題：面試專案簡報

### 功能投影片（共 10 張）
每張投影片對應一個主要功能模組，包含：
- 功能標題
- 左側截圖區域
- 右側功能描述（5-7 個要點）

### 技術投影片（3 張）
- 技術特色
- 資料庫設計
- 專案總結

## 快速製作流程

1. 啟動應用程式
2. 逐一訪問各個頁面並截圖
3. 開啟 HTML 檔案或生成的 PPTX
4. 將截圖插入對應位置
5. 檢查文字描述是否準確
6. 儲存為最終的 PPTX 檔案

